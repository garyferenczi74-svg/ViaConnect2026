Prompt 159 pre-flight inspection — PARTIAL (live DB unreachable from this session)

DATE: 2026-05-11 UTC
EXECUTOR: Michelangelo (TDD owner)
PROJECT: viaconnect-web
SUPABASE PROJECT ID: nnhkcufyqjojdbvdrpky
STATUS: HALTED at Step 1 per the prompt's hard-halt directive

================================================================================
HALT REASON
================================================================================
The prompt requires Section 8 pre-flight to run against the LIVE schema before any
migration is authored. This session has:
  1. No service_role key in .env.local (only NEXT_PUBLIC_SUPABASE_ANON_KEY + NEXT_PUBLIC_SUPABASE_URL).
  2. No SUPABASE_DB_URL or DATABASE_URL in any .env* file present in the project.
  3. No psql binary on PATH (Git Bash on Windows).
  4. Supabase CLI present (v2.84.2) and project linked locally (supabase/.temp/project-ref
     = nnhkcufyqjojdbvdrpky), but `supabase projects list` returns Unauthorized — no
     authenticated CLI session.
  5. No Supabase MCP tools surfaced in this session's function list (only
     Read/Grep/Glob/Edit/Write/Bash/WebFetch/WebSearch + NPI Registry MCP).

Per the prompt's stop condition ("If pre-flight cannot reach the DB, halt and report.
Do not author the migration."), Steps 2 through 6 are NOT executed.

================================================================================
PARTIAL FINDINGS FROM FILESYSTEM EVIDENCE
================================================================================
These are derived from the local migration files and the generated
src/lib/supabase/types.ts. They are a strong proxy for live schema in a healthy
repo but are NOT a substitute for a live `\d` against production.

INSPECTION 1: bio_optimization_history columns + types
  Source: supabase/migrations/20260402_profile_bio_columns.sql lines 65-78,
          src/lib/supabase/types.ts lines 518-547
  Result:
    id           uuid       PK
    user_id      uuid       FK -> auth.users(id) ON DELETE CASCADE, NULL per types.ts
    date         date       NOT NULL
    score        numeric(5,2) NOT NULL    -- matches §6.1 spec assumption
    source       text       default 'caq_initial'
    breakdown    jsonb      default '{}'::jsonb
    created_at   timestamptz default now()
  Unique constraint: (user_id, date)
  RLS: enabled, policy "Users manage own bio history" FOR ALL using auth.uid() = user_id
  COLUMNS NOT PRESENT (spec assumes they will be added in this migration):
    tier              -- §6.2 plans to ADD
    confidence        -- §6.2 plans to ADD
    inputs jsonb      -- §6.2 plans to ADD
    compute_version   -- §6.2 plans to ADD
    computed_at       -- §6.2 plans to ADD
  Assumption HOLDS: score is numeric(5,2) (no integer downcast needed).

INSPECTION 2: profiles.bio_optimization_score
  Source: migrations show profiles.bio_optimization_score declared as NUMERIC
          in 20260407000061 / 20260407000063 / 20260420000001
  Result: column exists, NUMERIC type. Spec §6.4 projection writes a score back
          to this column — typing matches.
  Assumption HOLDS.

INSPECTION 3: health_scores shape
  Source: src/lib/supabase/types.ts lines 5369-5400
  ACTUAL columns: id, user_id, score, calculation_version, created_at
  ACTUAL relationships: only foreign key to profiles(id) — note this is profiles
          NOT auth.users, which is itself a divergence from the codebase's usual
          FK target.
  MAJOR DEVIATION from §6.5 spec:
    - NO `score_type` discriminator column. Spec said "where score_type = 'bio_optimization'".
    - NO `recorded_at` column. Spec assumed `(user_id, score_type, recorded_at)` UNIQUE.
    - No corresponding CREATE TABLE in any local migration file — the table must
      have been created out-of-band or by a migration applied directly to live.
  ACTION REQUIRED before migration authoring:
    - Confirm via live DB inspection: does `score_type` actually exist? Does any
      ordering column besides created_at exist?
    - If §6.5's projection-to-health_scores depends on a column that does not
      exist, the spec must EITHER add the discriminator columns in this migration
      OR drop the health_scores projection from project_bio_optimization_score.
  Cannot resolve without live DB.

INSPECTION 4: daily_scores shape
  Source: supabase/migrations/20260413000020_daily_scores_rebuild.sql lines 29-45
  Result:
    id, user_id (FK auth.users), score_date date, sleep_score integer 0-100,
    energy_score integer 0-100, mood_stress_score integer 0-100, nutrition_score
    integer 0-100, activity_score integer 0-100, overall_score integer 0-100,
    data_mode text CHECK in (manual, wearable, combined), manual_completeness
    numeric(3,2), wearable_completeness numeric(3,2), source_breakdown jsonb,
    calculated_at timestamptz
  Unique constraint: (user_id, score_date)
  RLS: enabled, "Users can manage own daily scores"
  Index: idx_daily_scores_user_date on (user_id, score_date DESC)
  DEVIATION from §6.5 spec:
    - overall_score is INTEGER 0-100, but bio_optimization_history.score is
      NUMERIC(5,2). Projection write must ROUND().
    - daily_scores.data_mode is NOT NULL with a CHECK constraint. The projection
      must specify a data_mode value (probably 'combined' for BOS-derived rows
      or a new sentinel like 'bos_projection' — but adding a new sentinel
      requires altering the CHECK, which would impact existing rows).
  ACTION REQUIRED:
    - Decide on data_mode value for projected rows. If 'combined' is acceptable,
      the projection works as-is. If a new sentinel is needed, the migration
      must drop+re-add the CHECK constraint.
    - Gary decision required.
  Spec assumption (UNIQUE (user_id, score_date)) HOLDS.

INSPECTION 5: baseline row count for bio_optimization_history
  Cannot resolve without live DB. The no-data-loss assertion in
  06_audit_completeness.sql will need this baseline at migration time.

INSPECTION 6: latest 5 bio_optimization_history rows
  Cannot resolve without live DB. Local migration evidence indicates the new
  spec columns (tier, confidence, inputs, compute_version, computed_at) do not
  yet exist, so prior data has no values for these — the backfill plan must
  hydrate them from `breakdown` JSON or a sentinel like `compute_version = 'legacy-v0'`.

INSPECTION 7: app-layer write site catalog
  See tmp/159/write_sites.md for the deliverable to Prompt 160.

================================================================================
SECONDARY EVIDENCE (NOT A FAILURE BUT WORTH NOTING)
================================================================================

Spec §6.5 health_scores projection ASSUMES a score_type column. The actual
shape in types.ts has calculation_version as the only descriptive text column.
If `calculation_version` is being repurposed as a discriminator (e.g., values
like 'bio_optimization' vs 'engagement'), that is itself a code-smell because
calculation_version is supposed to track algorithm version. This needs Gary's
clarification.

Spec §6.4 references `auth.uid()` and `auth.role()` patterns. Repo has a
mature SECURITY DEFINER convention in 20260407000060_protocol_sharing.sql and
20260407000063_provider_list_shared_patients.sql — Step 2 should mine those
files for the canonical role-discrimination idiom before authoring. (Cannot
complete Step 2 in this halt state.)

The Marshall dictionary scanner concern is N/A for DB-layer work — no public
copy is being authored in this prompt.

================================================================================
WHAT GARY NEEDS TO UNBLOCK
================================================================================
ONE of the following:
  (A) Add SUPABASE_DB_URL (postgresql://...) to .env.local OR a separate file
      this session can read (e.g., .env.preflight). Format:
        postgresql://postgres.<project_ref>:<password>@<region>.pooler.supabase.com:6543/postgres
      OR the direct connection at port 5432.
  (B) Run `supabase login` in Gary's shell to authorize the linked CLI, then
      this agent can use `supabase db remote` commands.
  (C) Enable the Supabase MCP tool (`execute_sql`) in this session's tool list.
  (D) Run the seven §8 inspections in Gary's shell and paste the raw output
      into a tmp/159/preflight_live.txt for this agent to read.

Once any of (A) through (D) is in place, this agent resumes at Step 1 and
proceeds through Steps 2 to 6 (tests-red, migration, tests-green, idempotency,
docs, PR description) in one go.
