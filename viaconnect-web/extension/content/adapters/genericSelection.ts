/// <reference types="chrome" />
/**
 * Generic selection adapter — the right-click context-menu path.
 * No composer discovery; pulls selection text on user invocation.
 * Handler lives in sw.ts (contextMenus.onClicked).
 */
export {};
