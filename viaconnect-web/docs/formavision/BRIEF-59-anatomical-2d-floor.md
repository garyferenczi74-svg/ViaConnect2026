# BRIEF 59 — Picasso-pack anatomical 2D floor (LOOK amend)

Picasso / Jeffery AMEND. Gary lock: “You sold me on this.”

## Locked answers

1. YES. The Gary-locked Picasso pack (plasma-teal muscle-line figures with real head / hands / feet) is the always-paint floor. Bundle Male + Female, front + rear, locally (not remote-only). Replaces stick LocalSilhouette and the #180 procedural SVG diagram.
2. 3D is NOT SVG-to-mesh. Keep parametric / morph from scan girths and BF. Visual bar = Picasso anatomical class + Brief 58 teal density / volume / rim.
3. Never-empty stays. WebGL dead → Picasso floor. Never stick. Never blank.

## Floor (product path)

- `FormaVisionAnatomicalFloor` renders the bundled Picasso PNGs by sex + view.
- Default view is **rear** (Brief 58 rear ¾). Front plates are available.
- Wired on recovering, pending, plate underlay, and fellBack children.
- Stick circle-head / oval-torso geometry is gone. Procedural SVG body paths are gone. `FormaVisionLocalSilhouette` is a compatibility alias of the Picasso floor only.
- No remote Supabase Male / Female Avatar.svg.

## Motion (MOTION-SPEC)

- Floor paints immediately (0ms).
- 3D ready: 420ms cubic-bezier(0.22, 1, 0.36, 1) floor opacity 1→0, morph_3d 0→1.
- Fallback reverse 240ms. Sex toggle 200ms. Enter plate 180ms ease-out (chrome only).
- See `docs/formavision/MOTION-SPEC.md`.

## 3D (unchanged class)

- Product mesh stays `scanToParamVector` + `BODY_BUILD_BY_TIER` (Brief 58 Phase 1).
- No SVG-to-photogrammetry conversion.
- Teal density / volume / rim from #178 stays.

## Honesty

- Landmark ticks only when real girths exist (`measured` / `overlay`). Estimate / none = no ticks.
- Fallback notice stays honest when the floor shows because 3D cannot run.

## KILLS

Circle head, pointed/spike arms, block/brick feet, mitten hands, lollipop neck, stick LocalSilhouette, blank plate.

## Out

- SVG-to-mesh
- Purple brand
- Abandoning never-empty
- `--prod`
- `package.json` unless Gary required
- Regressing #178 3D
