# BRIEF 59 — designed anatomical 2D floor (updated)

Picasso / Jeffery AMEND. Answer path LOCKED.

## Locked answers

1. YES. Designed muscle-line 2D (Gary second phone shot) is the always-paint floor and the anatomical quality bar. Bundle Male + Female locally (not remote-only). Replaces stick LocalSilhouette entirely.
2. 3D is NOT SVG-to-mesh. Keep parametric / morph from scan girths and BF. Visual bar = anatomical class of that 2D + Brief 58 teal density / volume / rim.
3. Never-empty stays. WebGL dead -> anatomical 2D. Never stick. Never blank.

## Floor (product path)

- `FormaVisionAnatomicalFloor` is the bundled Male / Female SVG (A-pose, ankle crop, soft volume, muscle-line, plasma teal chrome lock).
- Wired on recovering, pending, plate underlay, and fellBack children.
- Stick circle-head / oval-torso geometry is gone. `FormaVisionLocalSilhouette` is a compatibility alias of the anatomical floor only.
- No remote Supabase Male / Female Avatar.svg.

## 3D (unchanged class)

- Product mesh stays `scanToParamVector` + `BODY_BUILD_BY_TIER` (Brief 58 Phase 1).
- No SVG-to-photogrammetry conversion.
- Teal density / volume / rim from #178 stays.

## Honesty

- Landmark ticks only when real girths exist (`measured` / `overlay`). Estimate / none = no ticks.
- Fallback notice stays honest when the floor shows because 3D cannot run.

## Out

- SVG-to-mesh
- Purple brand
- Abandoning never-empty
- `--prod`
- `package.json` unless Gary required
- Regressing #178 3D
