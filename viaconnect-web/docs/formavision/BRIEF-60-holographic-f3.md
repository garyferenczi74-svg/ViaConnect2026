# BRIEF 60 — Ready settle is storyboard Frame 3 holographic grid

Gary + Arnold lock: FormaVision Ready success is **Frame 3** — a designed cyan holographic grid on real human topology. Not uncontrolled Picasso shards. Not an opaque solid as the final Ready look.

Storyboard: F1 anatomical → F2 hybrid half-morph → F3 holographic grid.

## Locked answers

1. **Ready success look is `holographic-f3`.** Parametric default via `makeBodyHolographicMaterial`. Meshy textured GLB remains an allowed mesh source (`meshy-glb`).
2. **#188 `makeBodySolidMaterial` is superseded as the Ready stamp.** Solid stays for ghost / compare overlays. Leaving solid as Ready success kills F3.
3. **Additive barycentric shards are Picasso — FAIL.** Designed continuous quad/tri grid (diagonals hidden) is PASS.
4. **Standing locks unchanged.** Zero teal alien / `FormaVisionAnatomicalFloor` / `LocalSilhouette`. Never-empty: live 3D or #187 text-only notice. SnapMeasure OBJ is not wired to Ready.

## Materials (F3)

- Topology: parametric scan-morph / adult proportions (`scanToParamVector` + `BODY_BUILD_BY_TIER`).
- Wireframe: continuous dense grid on the volume. Cinematic ~96 radial × 80 vertical.
- Line color: Chrome plasma teal `#2EE6D6` (not ZOZO purple; four brand tokens unchanged).
- Fill: dark translucent navy/card, opacity 0.25–0.40.
- Edge: soft emissive rim. NormalBlending, FrontSide, depthWrite. Not `THREE.wireframe`.
- Camera: rear default, FOV 35–40° (locked 38°). Ankle-crop distance retuned from the 30° hero.

## Motion (~800ms F1→F3)

| Beat | Duration | Easing |
| --- | --- | --- |
| Floor paint | 0ms | — |
| Half morph (F2 overlap) | 280ms | — |
| Crossfade to F3 | 420ms | cubic-bezier(0.22, 1, 0.36, 1) |
| Settle, then orbit unlock | 200ms | ease-out |

Enter 180 + crossfade 420 + settle 200 = 800ms. Half-morph overlaps the crossfade. If already 3D, honest F3 settle.

## KILLS

Picasso shards. Opaque solid as Ready success. Teal alien floor. Blank navy after deadline. SnapMeasure OBJ on Ready. Purple brand. `--prod` / `package.json`.
