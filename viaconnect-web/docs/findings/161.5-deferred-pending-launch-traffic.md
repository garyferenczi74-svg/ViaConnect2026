# Prompt #161.5: Deferred pending post-launch traffic

**Deferral decision:** Gary, 2026-05-12
**Path chosen:** (A) Wait until post-launch
**Reason:** Pre-flight Gate 2 cannot pass because the BOS telemetry pipeline shipped today and FarmCeutica's product launch is June 2026. There is no production canonical activity to observe.

## Pre-flight state at deferral time

| Gate | Spec requirement | Actual result 2026-05-12 |
|---|---|---|
| Gate 1 (legacy writes 7d) | `count(*) = 0` | 0 (passes trivially; telemetry table is empty) |
| Gate 2 (canonical writes 7d) | `count(*) >= 100` | 0 (FAILS; no production traffic yet) |
| Gate 3 (health_scores polymorphism) | inspect score_type distribution | N/A; spec mismatch (see below) |

## Spec mismatches surfaced

Prompt #161.5 was written against a mental model that does not match this project's implementation. Two corrections recorded for the eventual resume:

1. **`health_scores` is NOT a BOS projection target in this project.**
   - Spec §0.1 + §2.1 assume `health_scores` carries rows with `score_type = 'bio_optimization'` and that the projection trigger writes to it.
   - Per Q2 of #159's architectural decisions (Gary, 2026-05-11): "drop the health_scores projection from the trigger entirely."
   - Live schema: `health_scores (id, user_id, score, calculation_version, created_at)`, with no `score_type` column. Table is 0 rows. No projection target. No lockdown work needed for this surface.

2. **`daily_scores` projection column is `bio_optimization_score`, NOT `overall_score`.**
   - Spec §2.1 + §6.1 Block 3 reference `daily_scores.overall_score`.
   - Per Q3 of #159's architectural decisions (Gary, 2026-05-11): "trigger writes only to `daily_scores.bio_optimization_score` column. Existing `src/app/actions/dailyScores.ts` and the quick-daily-log + nutrition-log write paths retain full ownership of every other daily_scores column."
   - When #161.5 resumes, the lockdown statements target `daily_scores.bio_optimization_score`.

## Actual reduced-scope lockdown when #161.5 resumes

```sql
REVOKE UPDATE (bio_optimization_score) ON TABLE public.profiles
  FROM authenticated, anon;

REVOKE INSERT (bio_optimization_score) ON TABLE public.daily_scores
  FROM authenticated, anon;
REVOKE UPDATE (bio_optimization_score) ON TABLE public.daily_scores
  FROM authenticated, anon;

-- No health_scores lockdown (not a projection target in this project)
```

3 statements on 2 columns. No RLS policy needed (no polymorphic table involved). No `health_scores` work. The full migration still needs Sherlock + Kelsey + Gary sign-offs, the pre-flight findings doc, the runbook update, the architecture doc update, and the 4 pgTAP test files.

## Resume conditions

Re-run pre-flight before resuming. All four conditions must hold:

1. **Gate 1**: `SELECT count(*) FROM bos_write_telemetry WHERE occurred_at > now() - interval '7 days' AND is_canonical = false;` returns `0`
2. **Gate 2**: `SELECT count(*) FROM bos_write_telemetry WHERE occurred_at > now() - interval '7 days' AND is_canonical = true;` returns `>= 100`
3. **Sign-offs locked**: Sherlock (RLS strategy) + Kelsey (audit-trail completeness) + Gary (operational risk acceptance)
4. **Shadow DB verification**: migration applies cleanly to a Supabase branch + verification block passes

When all four hold, re-dispatch Michelangelo with the reduced-scope spec above plus the spec's full §6 / §7 / §10 structure.

## Suggested calendar trigger

Earliest realistic re-evaluation: **2026-06-22** (one week after the June 2026 product launch, assuming the launch goes live around 2026-06-15). At that point real CAQ submissions + daily logs should be generating queued events and the worker drain should be producing canonical writes.

If you want a cron-style reminder, the orchestrator can create a `/schedule` job that polls `bos_write_telemetry` weekly starting 2026-06-15 and pings when the gates clear.

## BOS workstream state at deferral

| Prompt | Status |
|---|---|
| #159 + #161 bundle | LIVE; migration applied; canonical compute path live |
| #161a XSS strip + queue limit | LIVE |
| #161b telemetry hardening | LIVE; this is the telemetry pipeline being observed |
| #161c constants + type widening | LIVE |
| #161d investigation findings | LIVE |
| #161d-fix UTC correctness | LIVE; migration applied |
| #162 Dashboard card UI redesign (rejected) | superseded by #161e |
| #161e BOS card corrective rewrite | LIVE; production deployed `c15c9d9` |
| **#161.5 projection-table RLS lockdown** | **DEFERRED: this document** |
| #161.5 follow-on (read API: weekly_delta + tracked_dimensions) | queued for next prompt |

After #161.5 lands post-launch, the SSOT §3.2 spec compliance loop closes.
