# Prompt #117 — App-Store Foundation Completion — Pre-Push Audit

**Date:** 2026-04-22
**Orchestrator:** Jeffery
**Lanes:** Michelangelo (TDD/Dev), Sherlock (Research), Hannah (UX), Arnold (Body Tracker)
**Commit scope:** 107 files staged (native `ios/` + `android/` projects from `cap add`, `public/icons/*.webp`, `public/manifest.json` bug-fix, `public/index.html` offline fallback, `capacitor.config.ts` tune-up)
**Adaptation note:** Gary's original 5-lane spec targeted Prompt #113 (regulatory compliance). Prompt #113 is not in-repo. The 5 lanes are adapted to #117 scope; Kelsey / jurisdiction / DSHEA content does not apply.

---

## Lane 1 — Jeffery (Orchestrator): cross-prompt integration

| Check | Result |
|---|---|
| `public/index.html` conflict with `src/app/page.tsx` root route | Clean. Next.js App Router serves `/` from `app/page.tsx`; `public/index.html` is served at `/index.html` literally, no collision. Capacitor consumes it as the webDir root. |
| `capacitor.config.ts` parses | Clean (cap sync succeeded; cap doctor ran). |
| Files staged within #117 scope | 107 files. Zero leakage into Supabase migrations, Supabase email templates, `package.json`, or existing `src/` business logic. |
| Composition with prior prompts | #109/#110 photo-sync untouched. Portal audit P0/P1 fixes (#117 prior commits) untouched. No cross-prompt regressions. |

**Verdict: GREEN.**

---

## Lane 2 — Michelangelo (TDD/Dev): code quality + standing-rule enforcement

| Check | Result |
|---|---|
| TypeScript strict (`tsc --noEmit`) on new files | Zero errors in `capacitor.config.ts`, `scripts/generate-app-store-assets.mjs`, `scripts/generate-pwa-icons.mjs`, `public/index.html`, `public/manifest.json`. |
| `@ts-ignore` in staged code | 0 |
| `: any` in staged code | 0 |
| `as unknown as X` in staged code | 0 |
| Emojis in authored text files | 0 (node regex scan across `capacitor.config.ts`, both scripts, runbook, `index.html` returns empty match set for ranges `U+1F300-1FAFF`, `U+2600-27BF`, `U+1F000-1F2FF`) |
| Lucide icons at `strokeWidth={1.5}` | N/A — no React components added this commit. Brand mark is an inline SVG (`<svg>` in offline index.html), a different visual category from UI icons. |
| Edits to `package.json` | None this commit. Prior commit `6074559` updated `package-lock.json` deterministically from `npm install` (no dep additions). |
| Edits to Supabase email templates | None. |
| Edits to previously applied migrations | None. |
| Vitest regression check | `tests/photo-sync-helpers.test.ts` 51/51 pass. No other test files in scope. |

**Verdict: GREEN.**

---

## Lane 3 — Sherlock (Research): data integrity + version compatibility

| Check | Result |
|---|---|
| Brand-token fidelity: generator vs source | `scripts/generate-app-store-assets.mjs` uses `#0B1120` / `#22D3EE` / `#B75F19` — identical match to `public/icon.svg`. Plus `#F5F7FB` ink for splash wordmark only. |
| Capacitor 6.2.x vs Next.js 14.2.35 | Supported combination. Capacitor 6 targets iOS 13+ / Android API 22+; Next 14 has no native-shell constraints. |
| Bundle ID uniqueness | `com.farmceutica.viaconnect` — reverse-DNS of parent entity. No collision with existing FarmCeutica or CedarGrowth bundles (none in repo to check; live App Store verification deferred to Gary's Apple Developer account scan). |
| Icon generation count | 7 PWA WebP (48, 72, 96, 128, 192, 256, 512), 87 Android density-bucket variants, 10 iOS icon+splash variants. All verified present on disk. |
| Manifest icon-path resolution | 7/7 paths resolve (`/icons/icon-{48,72,96,128,192,256,512}.webp` all exist under `public/icons/`). |

**Verdict: GREEN.**

---

## Lane 4 — Hannah (UX): design system + cross-page references

| Check | Result |
|---|---|
| Theme tokens intact | Deep Navy `#0B1120` used on splash, offline fallback, manifest `theme_color` + `background_color`. Teal `#22D3EE` on ring + tagline. Orange `#B75F19` on V letter. Match design system. |
| Offline fallback aesthetic matches splash | Yes. Same navy canvas, same V+ring mark, same "BUILT FOR YOUR BIOLOGY" tagline in teal. Transitions cleanly when Capacitor flips webview from network to fallback and back. |
| Manifest validates | Name, short_name, description preserved from original. Icons array uses absolute paths (fixed from capacitor-assets' broken `../icons/*`) and correct `image/webp` MIME (fixed from `image/png`). `display: standalone`, `orientation: portrait-primary`, `scope: /`, `start_url: /dashboard`. |
| Safe-area insets present | `src/app/globals.css` has `@media (display-mode: standalone)` block applying `env(safe-area-inset-*)` to `body`. Web experience unchanged (browser users don't trigger the media query). |
| DSHEA disclaimer enforcement | N/A — Prompt #117 does not touch regulatory disclaimer surfaces. |
| "Built For Your Biology" on export artifacts | Appears on splash + offline fallback. |

**Verdict: GREEN.**

---

## Lane 5 — Arnold (Body Tracker): symptom capture + Capacitor WebView compat

| Check | Result |
|---|---|
| Body Tracker files touched by this commit | **0.** `git diff --cached --name-only | grep -iE "body-tracker|arnold"` returns empty. |
| Arnold coaching flow disturbed | No. No code paths altered. |
| Jurisdiction gate (Retatrutide, RUO peptides) | N/A — not part of #117. |
| WebView compatibility with Body Tracker features | **Flagged for Phase 2 (not a #117 blocker):** Body Tracker uses `@mediapipe/pose`, `@mediapipe/selfie_segmentation`, `@tensorflow/tfjs`, and `html5-qrcode`. All are browser APIs (WebAssembly + getUserMedia). Capacitor WebView on iOS uses WKWebView which supports both. On Android, Capacitor uses the system WebView which supports them on API 24+ (our minimum). Camera permission needs to be declared in `ios/App/App/Info.plist` (`NSCameraUsageDescription`) + `android/app/src/main/AndroidManifest.xml` (`<uses-permission android:name="android.permission.CAMERA"/>`). Gary will hit this when he first runs Body Tracker in the native shell. Add to the TestFlight / Play Internal testing checklist. |

**Verdict: GREEN for this commit; advisory for Phase 2.**

---

## Acceptance criteria roll-up

| Criterion | Status |
|---|---|
| Each lane returns zero P0/P1 | ✓ five lanes green |
| `npx vitest run` passes | ✓ 51/51 |
| `npx tsc --noEmit` passes on new files | ✓ zero errors |
| `npx cap doctor` | ✓ Android OK; iOS error is Windows-only (`Xcode is not installed`), which Gary resolves on his Mac |
| Vercel preview deploy | Deferred — cannot run from this sandbox; Vercel will build on push |
| Rollback path (`COMPLIANCE_KELSEY_ENABLED`) | N/A for #117 |
| Migration idempotency | N/A — no DB migrations in #117 |
| Audit doc committed | This file, `docs/audits/prompt-117-audit-2026-04-22.md`, bundled into the push |

---

## Recommended commit message

```
feat(#117): app-store foundation — ios + android native projects + PWA
```

## Recommended post-push actions (Gary)

1. Watch the Vercel preview deploy. PWA manifest should validate in Chrome DevTools → Application → Manifest with zero warnings.
2. Open `ios/App/App.xcworkspace` on Mac after running `cd ios/App && pod install`. Configure signing team under Signing & Capabilities.
3. Install JDK 17 + Android Studio. Open `android/` in Android Studio; accept Gradle sync.
4. First-run camera-permission flow for Body Tracker — verify the WebView prompts correctly on each platform (add `NSCameraUsageDescription` to `Info.plist`, `android.permission.CAMERA` to `AndroidManifest.xml`).
5. Set up TestFlight internal testing group (Gary, Dr. Dagher, Thomas Rosengren) per `docs/runbooks/app-store-deployment.md` §3 "Developer account identifiers."

## Known deferred items (not blockers)

- **iOS `pod install`**: skipped during `cap sync` (CocoaPods is macOS-only). Gary runs once on Mac before Xcode opens the workspace.
- **Android Gradle sync**: skipped during `cap add android` (no JDK on this sandbox). Android Studio prompts on first open.
- **Push notifications (APNs + FCM)**: explicitly deferred to Phase 2 per runbook §4.
- **Deep links (Universal Links + App Links)**: deferred to Phase 2.
- **Instrument Sans tagline on splash**: renders as fallback font (Instrument Sans not installed on this Windows sandbox). Not a blocker; Gary can regenerate on Mac with the font installed, or tweak via Canva path.
