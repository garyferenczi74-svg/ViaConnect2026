/**
 * Prompt 214d Gap 3: peptide PDP retired; 301 equivalent via redirect to education.
 */

import { redirect } from 'next/navigation';

export default function ShopPeptideSlugRetiredPage() {
  redirect('/peptide-protocol');
}
