/** @type {import('next').NextConfig} */
// Root-level config placeholder for Vercel framework detection.
// The actual Next.js project lives in viaconnect-web/ and is built via
// the custom buildCommand in vercel.json.
const nextConfig = {};
export default nextConfig;
